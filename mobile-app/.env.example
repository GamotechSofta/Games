# Expo mobile app – loaded at build/start (EXPO_PUBLIC_*)
# Local backend (emulator / same machine)
EXPO_PUBLIC_API_BASE_URL=http://localhost:3010/api/v1

# Physical device on LAN: use your PC IP instead of localhost, e.g.:
# EXPO_PUBLIC_API_BASE_URL=http://192.168.1.10:3010/api/v1
