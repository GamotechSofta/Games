export default function Settings() {
  return (
    <div className="page-placeholder">
      <h1>Settings</h1>
      <p className="text-muted">Screen: settings</p>
      <p>Settings (secret declare password, UPI, etc.) are available only in the Super Admin panel.</p>
    </div>
  );
}
