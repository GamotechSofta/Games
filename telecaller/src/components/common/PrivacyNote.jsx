const PrivacyNote = () => (
    <p className="text-xs text-gray-500 mb-4">
        Player contact and activity times only — no wallet balance or payment amounts.
    </p>
);

export default PrivacyNote;
