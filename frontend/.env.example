# Frontend (legacy) – Vite (npm run dev)
# Backend API base (no /api/v1 suffix – used as axios baseURL root)
VITE_API_BASE_URL=http://localhost:3010/api/v1

# Optional: separate admin API if different from main backend
# VITE_ADMIN_API_BASE_URL=http://localhost:3010
