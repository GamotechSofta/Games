# Copy to .env and set your backend URL (same as frontend)
# For web / same machine: use localhost
# For phone or emulator: use your computer's IP (e.g. 192.168.1.5) so the device can reach the server
EXPO_PUBLIC_API_BASE_URL=http://localhost:3010/api/v1
